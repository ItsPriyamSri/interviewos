{
  "name": "interviewos",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "InterviewOS MCP server: ingest_jd (read) and publish_workspace (write, approval-gated)",
  "main": "mcp/server.mjs",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "start": "node mcp/server.mjs"
  },
  "engines": {
    "node": ">=22.14"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  }
}
